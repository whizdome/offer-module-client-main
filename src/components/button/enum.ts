export enum ButtonState {
	PRIMARY = "bg-ap-success flex justify-center font-semibold items-center gap-2 text-white text-xs",
	OUTLINE = "bg-transparent border border-ap-success flex justify-center font-semibold items-center gap-2 text-ap-success text-xs",
}

export enum ButtonSize {
	lg = "px-4 h-[44px]",
}
